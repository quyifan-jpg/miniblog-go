[pytest]
testpaths = tests
python_files = test_*.py
asyncio_mode = auto
# Scripts that happen to live under tests/ but are CLI debug runners, not pytest tests
norecursedirs = tests/__pycache__
addopts = --ignore=tests/test_langgraph_pipeline.py --ignore=tests/test_search_agent.py
